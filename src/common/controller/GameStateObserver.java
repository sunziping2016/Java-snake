package common.controller;

import java.util.Observer;

/**
 * Created by sun on 4/27/16.
 *
 * Listen to the gameState.
 *
 * Implemented by `GameStateSender` and `GameView`.
 */

/**
 * Exit when `update(..., null)`.
 */
public interface GameStateObserver extends Observer {
}
